.panel {
  display: flex;
  flex-flow: row wrap;
  flex: 1 0 100%;
  justify-content: center;
  align-items: center;
  margin-bottom: $base-spacing;

  .headline { margin-top: 0; }

  .tagline {
    max-width: 36em;
    font-size: $base-font-size * 1.3;
    padding: 0 $small-spacing;
    text-align: center;
  }

  p {
    margin-bottom: $small-spacing;

    &.last { margin: 0; }
  }

  h4 {
    font-weight: $base-font-weight;
    font-size: $base-font-size;
    margin: 0;
  }



  &.feed {
    background: $off-white;
    padding: 0 $base-spacing;
  }

  &.divide {
    position: relative;
    padding: 0;

    hr {
      width:100%;
      margin: 0;
    }

    span {
      position: relative;
      display: inline-block;
      background: $off-white;
      top: -24px;
      padding: $small-spacing;
      border: 1px solid #ccc;
      border-radius: 3px;
    }
  }

  &.last { margin-bottom: 0; }

  &.alternate {
    background: url("<%= image_path('back-light.png') %>");
    padding: $base-spacing * 1.5 0;
  }

  &.border-top { border-top: 1px solid $light-gray; }
  &.border-bottom { border-bottom: 1px solid $light-gray; }

  &.control {
    justify-content: space-around;
  }

  &.home {
    margin: 0;
    padding: $base-spacing $base-spacing 0 $base-spacing;

    .intro-head {
      display: block;
      font-size: $base-font-size * 1.25;
      font-weight: $base-font-weight * 2;
      padding: $small-spacing 0 $small-spacing * 0.5 0;
      width: 100%;
      text-align: center;
    }

    .button {
      max-width: 300px;
    }
  }
}
