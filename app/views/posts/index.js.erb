// Append new data
$("<%=j render(@posts) %>").appendTo($("#wrap .feed"));

// Update pagination link
<% if @posts.last_page? %>
  $('.pagination').html("Fin.");
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@posts, 'See more', :remote => true) %>");
<% end %>